services:
  pet-connect:
    build:
      context: /opt/services/pet-connect
      dockerfile: Dockerfile
    container_name: pet-connect
    environment:
      DATA_DIR: /data
      PORT: "8787"
    restart: unless-stopped
    volumes:
      - ./data:/data
    networks:
      - public

networks:
  public:
    external: true
